# Kaia Network Configuration
# Copy this file to .env and fill in your values

# Private key for deployment (without 0x prefix)
# NEVER commit your actual private key to version control!
PRIVATE_KEY=your_private_key_here_without_0x

# RPC URLs (Optional - defaults are provided in hardhat.config.js)
# You can use public endpoints or your own nodes
KAIA_RPC_URL=https://public-en.node.kaia.io
KAIROS_RPC_URL=https://public-en-kairos.node.kaia.io

# Optional: Custom RPC endpoints for better performance
# KAIA_RPC_URL=https://your-custom-kaia-node.com
# KAIROS_RPC_URL=https://your-custom-kairos-node.com

# Deployment configuration
DEPLOYER_ADDRESS=
GAS_LIMIT=8000000
GAS_PRICE=250000000000

# Contract verification on Kairos/Kaia Explorer
# Note: Kairos (testnet) and Kaia (mainnet) don't require API keys for verification
# The verification is done through Klaytnscope's web interface or hardhat-verify plugin
# Leave these empty - they're included for compatibility but not required
KAIROS_API_KEY=
KAIA_API_KEY=